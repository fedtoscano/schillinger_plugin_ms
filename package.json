{
  "name": "schillinger",
  "version": "1.0.0",
  "type": "module",
  "description": "MuseScore 4 Plugin - Schillinger System for rhythmic generation",
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",
    "esbuild": "^0.19.0",
    "jest": "^29.0.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  }
}
